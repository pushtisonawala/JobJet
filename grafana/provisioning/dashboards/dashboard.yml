apiVersion: 1

providers:
  - name: jobqueue-dashboards
    orgId: 1
    folder: JobQueue
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
